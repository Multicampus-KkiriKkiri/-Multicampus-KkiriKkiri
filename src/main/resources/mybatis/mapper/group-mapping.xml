<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.GroupDAO">
	
	<select id="getGroupDetail" resultType="groupdto">
		select * from `group` where groupId = #{groupId}
	</select>
	
	<!-- 모든 지역 가져오기 -->
    <select id="getAllRegions" resultType="dto.RegionDTO">
        SELECT regionId, regionName
        FROM regions
    </select>

    <!-- 특정 지역의 모든 구 가져오기 -->
    <select id="getDistrictsByRegionId" parameterType="int" resultType="dto.DistrictDTO">
        SELECT districtId, regionId, districtName
        FROM districts
        WHERE regionId = #{regionId}
    </select>
    
    <insert id="insertGroup" parameterType="dto.GroupDTO">
    INSERT INTO `group` (groupLeaderId, groupName, groupDetail, groupImage, groupType, groupRegion, groupInterest, groupSignUpType, groupMaximum, groupSignUpQuestion)
    VALUES (#{groupLeaderId}, #{groupName}, #{groupDetail}, #{groupImage}, #{groupType}, #{groupRegion}, #{groupInterest}, #{groupSignUpType}, #{groupMaximum}, #{groupSignUpQuestion})
	</insert>
  
</mapper>